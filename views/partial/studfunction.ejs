<script>
  $(document).ready(function() {
    $('[data-toggle="tooltip"]').tooltip();
  });
</script>
<nav class="navbar navbar-expand-lg navbar-dark bg-primary mb-4">
    <div class="container">
        <a class="navbar-brand" href="/student/home">Student Dashboard</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#studentNavbar">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="studentNavbar">
            <ul class="navbar-nav ml-auto">
                <li class="nav-item">
                    <a class="nav-link" href="/student/home">
                        <i class="fas fa-home"></i> Home
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/student/<%= currentUser._id %>/apply">
                        <i class="fas fa-file-alt"></i> Apply Leave
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/student/<%= currentUser._id %>/track">
                        <i class="fas fa-clock"></i> Track Leave
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/logout">
                        <i class="fas fa-sign-out-alt"></i> Logout
                    </a>
                </li>
            </ul>
        </div>
    </div>
</nav>
<h1 style="margin-top: 61px; font-size: 20px;">Welcome, <%=student.name%></h1>
<div class="container-fluid">
  <div class="row">
    <div class="col-md-3 col-sm-12">
      <div class="card">
        <% if(student.image) { %>
          <img src="<%= student.image %>" class="card-img-top" alt="Profile Image">
        <% } %>
        <div class="card-body">
          <h5 class="card-title"><%= student.name %></h5>
          <p class="card-text">Department: <%= student.department %></p>
          <p class="card-text">Hostel: <%= student.hostel %></p>
          <div class="d-grid gap-2">
            <a href="/student/<%= student._id %>" class="btn btn-info">Profile</a>
            <a href="/student/<%= student._id %>/apply" class="btn btn-primary">Apply Leave</a>
            <a href="/student/<%= student._id %>/track" class="btn btn-secondary">Track Leave</a>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
